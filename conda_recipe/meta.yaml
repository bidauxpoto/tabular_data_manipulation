{% set name = "tabular_data_manipulation" %} #no spaces
{% set version = "1.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source: 
  path: ../src

requirements:
  host:
    - perl
    - python>=3
  run:
    - perl
    - python>=3

about:
  home: https://github.com/bidauxpoto/tabular_data_manipulation 
  summary: Unix power tools to manipuate data in tabular format

#Simple python packages can work as simple noarch
#build:
#    noarch: python
